# Required by AGP's generateLocaleConfig (app/build.gradle.kts) to disambiguate which
# locale the unqualified `values/` directory represents — without this, AGP can't tell
# whether values/ is meant to be a fallback or a real "en" locale for the generated
# LocaleConfig.
unqualifiedResLocale=en
